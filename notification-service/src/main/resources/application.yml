spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost:3306/ai-recruitment-platform?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata
    username: root
    password: rootAdmin
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      pool-name: Hikari-notification-service
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000

  jpa:
    # MySQL8Dialect is deprecated in Hibernate 6
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none         
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Kolkata


server:
  port: 8084

# ---------------- Swagger / OpenAPI ----------------
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

# ---------------- Logging ----------------
logging:
  level:
    root: INFO
    liquibase: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
